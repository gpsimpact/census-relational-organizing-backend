type TibApplication {
  id: String!
  text: String!
  isApplied: Boolean!
  appliedAt: GraphQLDateTime
  tibType: TibType!
}

type TibTotal {
  id: String!
  text: String!
  count: Int!
}

enum TibType {
  QUESTION
  ACTION
}
