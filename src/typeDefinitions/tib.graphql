type TibApplication {
  id: String!
  text: String!
  isApplied: Boolean!
  appliedAt: GraphQLDateTime
  tibType: TibType!
}

type TibTotal {
  id: String!
  text: String!
  appliedCount: Int!
  unappliedCount: Int!
  tibType: TibType!
}

enum TibType {
  QUESTION
  ACTION
}
