type TibApplication {
  id: String!
  text: String!
  isApplied: Boolean!
  appliedAt: GraphQLDateTime
}

type TibTotal {
  id: String!
  text: String!
  count: Int!
}

enum TibType {
  QUESTION
  ACTION
}
