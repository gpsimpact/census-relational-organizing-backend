type Query {
  hello(name: String): String!
  me: User
  users(input: UsersInput): UsersResults!
  user(id: String, email: String): User
  client(id: String, slug: String): Client
  clients(input: ClientsInput): ClientsResults!
  io(id: String!): io
  ios(input: IOsInput): IOsResults!
}
