enum GlobalPermissionsEnum {
  ADMIN
  ADMIN_TEAMS
  ADMIN_TEAMS_CRUD
  ADMIN_USERS
  ADMIN_USERS_CRUD
}

enum SortDirection {
  ASC
  DESC
}

enum MutationCodeEnum {
  DUPLICATE
  DOES_NOT_EXIST
  INPUT_ERROR
  UNKNOWN_ERROR
  OK
}

scalar GraphQLDateTime
scalar GraphQLDate
scalar GraphQLTime
