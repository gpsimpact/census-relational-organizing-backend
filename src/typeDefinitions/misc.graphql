enum SortDirection {
  ASC
  DESC
}

enum MutationCodeEnum {
  DUPLICATE
  DOES_NOT_EXIST
  INPUT_ERROR
  UNKNOWN_ERROR
  OK
  EXPIRED
  INELIGIBLE
}

scalar GraphQLDateTime
scalar GraphQLDate
scalar GraphQLTime
